# Release version constants. The build/patch number is auto-incremented by
# .github/workflows/release.yml on every merge of main into the release branch:
#   v{VERSION_MAJOR}.{VERSION_MINOR}.{next build}
# Bump these manually (committed to main) to change the release line.
VERSION_MAJOR=0
VERSION_MINOR=1
